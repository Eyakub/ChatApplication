<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_profile"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#262121"
    tools:context="com.example.user.chatapplication.ProfileActivity">

   <ImageView
       android:layout_width="match_parent"
       android:layout_height="200dp"
       android:id="@+id/user_profile_image"
       android:src="@drawable/proimg"
       android:fitsSystemWindows="true"
       />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Display Name"
        android:layout_below="@+id/user_profile_image"
        android:layout_centerHorizontal="true"
        android:textSize="18dp"
        android:textColor="#fff"
        android:layout_marginTop="30dp"
        android:id="@+id/profile_user_name"
        />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="profile status"
        android:layout_below="@+id/profile_user_name"
        android:layout_centerHorizontal="true"
        android:textSize="15dp"
        android:textColor="#fff"
        android:layout_marginTop="10dp"
        android:id="@+id/profile_status"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="your friends number"
        android:layout_below="@+id/profile_status"
        android:layout_centerHorizontal="true"
        android:textSize="15dp"
        android:textColor="#fff"
        android:layout_marginTop="30dp"
        android:id="@+id/profile_user_friends"
        />


    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="sent friend request"
        android:background="@color/colorAccent"
        android:textSize="20dp"
        android:textColor="#fff"
        android:id="@+id/profile_friend_request"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_marginTop="30dp"
        android:layout_below="@+id/profile_user_friends"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="decline friend request"
        android:layout_margin="10dp"
        android:layout_below="@id/profile_friend_request"
        android:background="@color/colorAccent"
        android:textSize="20dp"
        android:textColor="#fff"
        android:id="@+id/profile_friend_request_decline"
        />

</RelativeLayout>
